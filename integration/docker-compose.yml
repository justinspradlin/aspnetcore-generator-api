version: '3.1'

services:
  generator:
    image: my-registry:55000/gen:ci-10
    # Don't publish ports in CI because multiple running version could have port conflicts
    #ports:
    #  - "8080:80"
  mail:
    image: mailhog/mailhog
    #ports:
    #  - "8025:8025"
  integration:
    image: my-registry:55000/gen:integration-10
    build: .